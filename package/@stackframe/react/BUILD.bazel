load("@aspect_rules_js//npm:defs.bzl", "npm_package")

exports_files(["package.json"])

# First-party file: dependency consumed by the generated pnpm link graph.
npm_package(
    name = "pkg",
    srcs = glob(["**"]),
    visibility = ["//visibility:public"],
)
