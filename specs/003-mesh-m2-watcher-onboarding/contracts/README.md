# Frozen contract surfaces — mesh-m2-watcher-onboarding (M2)
The M3 build-against boundary (FR-021). Breaking changes after M2 merges are defects; additive optional fields OK. Runtime in `src/crm/agents/` + `src/crm/ui/` must satisfy these; a type-assignability test pins it.
