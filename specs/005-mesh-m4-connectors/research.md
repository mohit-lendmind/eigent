# Research: mesh-m4-connectors (decisions)
D1 buildQuery=declarative plan run by the existing parked-delegation pump (no lb4Driver; executor fed only by aionChatBridge.onState). D2 MSE isolated/console-fetch; licensed logged-in DOM-scrape only (CDP_ALLOWED has no Network; console-exec blocked in logged_in). D3 replay=capture tool_result SSE→pure extract() in CI; live MSE canary for drift (halifax-style live evals unrunnable in CI). D4 snapshot=folded summary case-log entry + full-set attachment (fold drops oversize inline). D5 dedicated payload decoder (generic decodeSourcingSnapshot validates only spine). D6 verified derived from VerificationRef; single assertClaimable choke-point in writer/fold. D7 surfaceClass adviser-only + no-client-embed CI test (PERG 4.6). D8 coverage typed + wholeOfMarket bool + lint gate on the literal phrase (MCOB 4.4A); copied into every artifact. D9 v1=mse+mortgage-brain; framework supports all broker tools. D10 local sourcing serialized (singleton window); fixtures scrub tokens/CSRF/PII; writeBack/DIP→G8.
