# Research: mesh-m3-docintel (decisions)
D1 extraction=side-car; state deltas=case-log kinds w/ origin.artifactId (fold quarantines the extraction kind otherwise). D2 ids=pure fn(documentId,contentHash,fieldKey) (fold dedupes only by seq; dispatch nonce won't dedupe). D3 conflict+0.85 attribution=deterministic (preserves refold-byte-equality); agent proposes attribution features only. D4 quote-locator=coded substring vs born-digital text; flip src??'det'; vision-only⇒syn. D5 ingest seam FileAttachment→uploadAttachment→directive.inputs.artifacts is net-new. D6 red-team targets WRITE path (false-det/attribution-leak/suppression/outbound), closed schema, fixture+stub in CI. D7 per-field precision; income adviser-confirmed at G9 regardless; syn never satisfies G9. D8 data residency: synthetic-only tonight, real PII gated (see decisions/data-residency). D9 accuracy gate split: synthetic+red-team=hard blocker; ≥0.95-on-real gates claim+G9-autonomy only.
