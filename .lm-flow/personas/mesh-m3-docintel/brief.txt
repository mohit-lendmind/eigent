Build `mesh-m3-docintel`: the A3 document-intelligence agent + a doc vault surface, on `lendmind-crm`, developing against the frozen M2 dispatch/envelope contracts (M2 merge only needed for the vault verification surface) and consuming M1's fold + `lm.docintel.extraction/1` decoder. Authoritative design: `.lm-flow/architecture/mesh-m3-docintel.md` (corrected by this panel on five points below). System contract: spec v2 §§4-A3,5,7,9. Decisions [[connector-access]], [[data-residency]] bind. No new deps; `src/api/aion/v1/**` + M1/M2 frozen contracts frozen; CI gates green; **build + verify on synthetic/redacted fixtures only — no real client PII tonight**.

Corrections the spec MUST encode: (1) the extraction artifact is a **side-car**; state deltas ride as case-log event kinds with `origin.artifactId` — never feed the extraction kind to the fold. (2) All derived ids are pure functions of `(documentId, contentHash, fieldKey)` — re-processing is idempotent. (3) Conflict materiality (1% over Pence) is a **deterministic recompute**, never LLM-emitted; the 0.85 attribution gate is deterministic; this preserves M1 refold-byte-equality. (4) Quote-locator is a **coded substring match against born-digital PDF text** (add a locator field to `DocInsight`); match ⇒ `det`, else/vision-only ⇒ `syn`; flip the `src ?? 'det'` default. (5) The ingest seam `FileAttachment → uploadAttachment → directive.inputs.artifacts` is **net-new** Phase-1 work.

Phases: P1 ingest seam + `lm-docintel` skill scaffold + the write-path red-team harness (fixture bytes + stubbed model, zero-tolerance CI) + the DPIA doc + the closed extraction schema. P2 classify + extract + deterministic attribution (G2). P3 deterministic conflict recompute (G3) + checklist reconcile + G9 income gate (syn never satisfies). P4 doc vault surface (DocCard state machine, det deep-link to highlighted span, syn non-color+confidence, G2/G3 cards decidable without opening, collapse unmapped, typed errors, aria-live) + per-field precision report + demo + gates. Hard requirements: the write-path red-team is a merge blocker; income adviser-confirmed at G9 regardless of score; special-category + out-of-scope docTypes gated; `lm.docintel.extraction/1` frozen as the fact-find provenance source for M4/M5; contract freeze under `specs/004-mesh-m3-docintel/contracts/`; the M1 kill-the-laptop convergence holds with docintel entries. Include a "## User Journeys" section with the three journeys above.
